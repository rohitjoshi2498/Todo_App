import React, { useContext } from 'react';
// import { UserContext } from '../contexts/UserContext';

const TodoList = () => {
//   const { user } = useContext(UserContext);

  return (
    <div>
      <h3>Todo List</h3>
      <ul>
        {/* Loop through the user's todos and render each todo */}
        {/* You can access the todos from the user object in the context */}
      </ul>
    </div>
  );
};

export default TodoList;
